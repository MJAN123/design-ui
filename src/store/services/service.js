import axios from 'axios';

export const api = {
  addUser(user) {
    // Api Call
    // return axios
    //   .get(`${BASE_URL}router`)
    //   .then((response) => {
    //     return response.data;
    //   });
    return user;
  },
};
